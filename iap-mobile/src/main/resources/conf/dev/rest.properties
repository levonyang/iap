# RestTemplate超时设置
rest.connect.timeout=10000

# iap-search模块的basic url
iap.search.base.url=http://localhost:8183/api

# iap-search 公司简介接口的url
iap.search.company.brief.url=${iap.search.base.url}/graph/brief

# 获取cluster id
iap.search.graph.cid.url=${iap.search.base.url}/graph/cid

# 获取cluster
iap.search.graph.cluster.url=${iap.search.base.url}/graph/cluster

iap.search.graph.entityPath.url=${iap.search.base.url}/graph/entity_paths